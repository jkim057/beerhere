import React, {useState} from 'react';

const Viewer = () => {
    const [views,setViews] = useState([]);
    return (
        <div>
            <p>VIEWER</p>
        </div>
    );
};

export default Viewer;